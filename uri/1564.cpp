#include <bits/stdc++.h>

int main () {
        int k;
        while(scanf("%d", &k) != EOF) {
                if     (k == 0) printf("vai ter copa!\n");
                else if (k > 0) printf("vai ter duas!\n");
        };
        return 0;
}
