#include <bits/stdc++.h>

int main () {
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; i++) {char input[200]; scanf("%s", input); }
        printf("Ciencia da Computacao\n");
        return 0;
}
